import React from "react";
import { Typography, Box } from "@mui/material";

const Reports = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h3"> Los Reports</Typography>
    </Box>
  );
};

export default Reports;