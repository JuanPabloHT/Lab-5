import React from "react";
import { Typography, Box } from "@mui/material";

const HomePage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h3">Esta es la Home Page :D </Typography>
    </Box>
  );
};

export default HomePage;